#!/bin/bash
# Build a complete .vsix (with solc bundled) and install it into VS Code.
# See scripts/package-vsix.js for why a custom packaging step is needed.
set -e
node "$(dirname "$0")/scripts/package-vsix.js" --install
